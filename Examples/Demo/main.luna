// demo program

import 'imported.luna'
import 'imported.luna'

const ANY_CONST 111

(func (x) x)

(lambdaCall (l) (l 'called lambda'))

(set_get_global_variable ()
    (set @global_variable 123)
    @global_variable
)

// examples 

//(run (lambdaCall (lambda (x) x)))
//(run (if (eq 1 5) (func 10) (func 20)))
(run (set_get_global_variable))
