// program for testing

import 'imported.luna'


(func (x) x)

(lambdaCall (l) (l 'called lambda'))

//(run (lambdaCall (lambda (x) x)))

//(run (func (INT_CONST FLOAT_CONST STR_CONST)))

(run (if (eq 1 1) (func 10) (func 20)))
